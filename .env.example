# Copy to .env for local development.

# Django's debug pages. Never 1 in production - startup refuses it.
DJANGO_PROBE_DEBUG=1

# Defaults to the PostgreSQL container `just docker-postgres` starts.
# DATABASE_URL=postgresql://postgres:postgres@localhost:55432/django_probe
